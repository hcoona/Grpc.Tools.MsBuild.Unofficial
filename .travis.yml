language: csharp

solution: Grpc.Tools.MsBuild.sln

sudo: false

matrix:
  include:
    - os: linux
      dotnet: 1.0.3
      mono: none
    - os: osx
      dotnet: 1.0.3
      mono: none
    - os: linux
      dotnet: 1.0.3
      mono: latest
    - os: osx
      dotnet: 1.0.3
      mono: latest

git:
  depth: 3

install:
  - git fetch --unshallow
  - dotnet restore

script:
  - msbuild Grpc.Tools.MsBuild.sln
